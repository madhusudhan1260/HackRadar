fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27
beautifulsoup4>=4.12
apscheduler>=3.10
bcrypt>=4.2

# Postgres driver. Unused with the default SQLite URL, but required in
# production — hosts hand out a postgres:// DATABASE_URL.
psycopg[binary]>=3.2

# Optional: LLM classification of vague listings (set ANTHROPIC_API_KEY).
# anthropic>=0.40
